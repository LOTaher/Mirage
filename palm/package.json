{
  "name": "palm",
  "version": "1.0.0",
  "main": "dist/app.js",
  "scripts": {
    "dev": "npm run build && node dist/app.js",
    "build": "tsc -p ."
  },
  "dependencies": {
    "@slack/bolt": "^3.15.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  }
}
